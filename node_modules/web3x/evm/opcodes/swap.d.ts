/// <reference types="node" />
import { OpCode } from '.';
import { EvmContext } from '../vm/evm-context';
declare class SwapOp implements OpCode {
    readonly code: number;
    private position;
    readonly mnemonic: string;
    readonly description: string;
    readonly gas = 3;
    readonly bytes: number;
    constructor(code: number, position: number);
    toString(params: Buffer): string;
    handle(context: EvmContext): void;
}
export declare const SwapOps: SwapOp[];
export {};
