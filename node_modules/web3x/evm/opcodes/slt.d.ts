/// <reference types="node" />
import { OpCode } from '.';
import { EvmContext } from '../vm/evm-context';
declare class SltOp implements OpCode {
    readonly code = 18;
    readonly mnemonic = "SLT";
    readonly description = "Signed less-than comparison";
    readonly gas = 3;
    readonly bytes = 1;
    toString(params: Buffer): string;
    slt(x: bigint, y: bigint): bigint;
    handle(context: EvmContext): void;
}
export declare const Slt: SltOp;
export {};
