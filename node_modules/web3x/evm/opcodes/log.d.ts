/// <reference types="node" />
import { OpCode } from '.';
import { EvmContext } from '../vm/evm-context';
declare class LogOp implements OpCode {
    readonly code: number;
    private topics;
    readonly mnemonic: string;
    readonly description: string;
    readonly gas: number;
    readonly bytes: number;
    constructor(code: number, topics: number);
    toString(params: Buffer): string;
    handle(context: EvmContext): void;
}
export declare const LogOps: LogOp[];
export {};
