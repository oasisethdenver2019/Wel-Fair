/// <reference types="node" />
import { Address } from '../../address';
import { EvmAccount } from '../world/evm-account';
export interface Log {
    address: Address;
    topics: Buffer[];
    data: Buffer;
}
export declare class TxSubstrate {
    selfDestructSet: EvmAccount[];
    logs: Log[];
    touchedAccounts: {
        [address: string]: EvmAccount;
    };
    refundBalance: bigint;
}
