import { TransactionRequest } from '../../formatters';
import { TransactionHash } from '../../types';
import { Wallet } from '../../wallet';
import { Blockchain } from '../blockchain';
import { WorldState } from '../world';
export declare function handleSendTransaction(worldState: WorldState, blockchain: Blockchain, txRequest: TransactionRequest, wallet: Wallet, blockDelay?: number): Promise<TransactionHash>;
