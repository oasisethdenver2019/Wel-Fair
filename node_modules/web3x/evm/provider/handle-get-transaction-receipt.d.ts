import { RawTransactionReceipt } from '../../formatters';
import { TransactionHash } from '../../types';
import { Blockchain } from '../blockchain';
export declare function handleGetTransactionReceipt(blockchain: Blockchain, transactionHash: TransactionHash): Promise<RawTransactionReceipt | null>;
