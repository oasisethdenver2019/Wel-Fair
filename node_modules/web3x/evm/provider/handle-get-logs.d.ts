import { LogRequest, LogResponse } from '../../formatters';
import { Blockchain } from '../blockchain';
export declare function getLogs(blockchain: Blockchain, logRequest: LogRequest): Promise<LogResponse[]>;
