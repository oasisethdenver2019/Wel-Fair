/// <reference types="node" />
import { Address } from '../../address';
import { TxSubstrate } from '../tx';
import { WorldState } from '../world';
export declare function contractCreation(worldState: WorldState, sender: Address, origin: Address, gas: bigint, gasPrice: bigint, value: bigint, initCode: Buffer, callDepth: number, modify: boolean): Promise<{
    contractAddress: Address;
    remainingGas: bigint;
    txSubstrate: TxSubstrate;
    status: boolean;
}>;
